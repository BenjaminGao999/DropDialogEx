<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/fl_root_dialog"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/darker_gray"
    android:clickable="true">

    <RelativeLayout
        android:id="@+id/rl_remind_dialog"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:layout_marginRight="@dimen/activity_horizontal_margin"
        android:background="@android:color/white"
        android:padding="@dimen/activity_vertical_margin">

        <TextView
            android:id="@+id/tv_reward_dialog"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:gravity="center_horizontal"
            android:lineSpacingExtra="@dimen/activity_vertical_margin"
            android:text="任务成功\n奖励已发放至您的账户\n积分+100" />

        <Button
            android:id="@+id/btn_confirm_dialog"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tv_reward_dialog"
            android:layout_centerInParent="true"
            android:inputType="text"
            android:text="确认" />
    </RelativeLayout>

</FrameLayout>